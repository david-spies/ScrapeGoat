# ScrapeGoat Backend Dependencies
# Install: pip install -r requirements.txt
# Then:    playwright install chromium

# HTML parsing
beautifulsoup4>=4.12.0
lxml>=4.9.0

# Browser automation (DynamicFetcher / DynamicSession)
playwright>=1.40.0

# Standard library modules used (no install needed):
# asyncio, http.server, http.cookiejar, ssl, json,
# urllib, threading, socketserver, random, time, re, copy, pathlib
